#!/usr/bin/env python
from setuptools import setup, find_packages

setup(name='cdp',
      description='IO and Processing tools for managing the back ends of Council Data Project supported cities. Additional tools for simple transcription tasks, topic analysis, and predicition.',
      author='Jackson Maxfield Brown, Dr. Nicholas Weber',
      author_email='jmaxfieldbrown@gmail.com',
      url='https://github.com/CouncilDataProject/cdp',
      packages=find_packages(),
      install_requires=[])
